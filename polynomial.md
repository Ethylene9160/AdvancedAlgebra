<p align='center'><font size=6><b>第一章 多项式</b></font></p>

# 数域

由复数组成的集合P，包括0和1，如果其中任意两个数的四则运算（除数非0）仍然是P中的数，则P称为一个数域。

例如，具有形式:
$$
\mathbb Q(\sqrt2): a+b\sqrt 2,~a\in \mathbb R,~b\in \mathbb R
$$
是一个数域。

> 证明：
> 
> 设有两个数字$a+b\sqrt 2,~c+d\sqrt 2$.
>
> 对乘法：
$$
\begin{aligned}
(a+b\sqrt 2)(c+d\sqrt 2)=(ac+2bd)+(ac+bc)\sqrt 2
\end{aligned}
$$
> 满足形式$a+b\sqrt2$;
> 
> 除法：
$$
\begin{aligned}
(a+b\sqrt 2)\div (c+d\sqrt 2)&=\frac{(a+b\sqrt 2)(c-d\sqrt 2)}{(c+d\sqrt 2)(c-d\sqrt2)}\\
&=\frac{ac-2bd}{a^2-2b^2}+\frac{ad-bc}{a^2-2b^2}\sqrt2
\end{aligned}
$$
> 仍然符合该形式。
> 
> 加法、减法很好证明。略去。

# 一元多项式

对非负整数n，
$$
a_nx^n+a_{n-1}x^{n-1}+...+a_1x+a_0
$$
其中，$a_0, a_1,...,a_n$全属于数域P，称为**系数在数域P中的一元多项式**。

# 除法和整除

一元多项式可以轻松完成四则运算中的加减乘操作，而除法操作则需要进行考虑。

例如：
$$
\begin{aligned}
f(x)&=3x^3+4x^2-5x+6\\
g(x)&=x^2-3x+1
\end{aligned}
$$
f除以g，可以进行操作：

![c1-polinomial-division1](imgs/c1-polinomial-division1.png)

因此可以如此思考，f除以g等于$3x+13$余$31x+7$。

**带余除法**：任意$f(x), g(x)\neq0$，一定满足：
$$
f(x)=q(x)g(x)+r(x)
$$
整除的概念类似于因式分解。r(x)=0即可。

**符号**：$\mid$表示整除符号，$\nmid$表示不能整除符号。例如，$r(x)=0\rightarrow g(x)\mid f(x),\text{else}~g(x)\nmid f(x)$

也就是说，如果b是a的整数倍，那么$a\mid b$；如果b不是a的整数倍，那么$a\nmid b$。

# 最大公因式

如果存在$d(x)$，满足：
$$
f(x)\mid d(x),g(x)\mid d(x)
$$
那么$d(x)$是$f(x),g(x)$的公因式；

如果$f(x),g(x)$的公因式都是$d(x)$的公因式，则$d(x)$是$f(x),g(x)$的最大公因式。通常，用$\left(f(x),g(x)\right)$来表示两个多项式中*首项系数是1的那个最大公因式*。

如果$\left(f(x),g(x)\right)=1$，称为**互质**。



# 因式分解定理

需要注意因式分解*需要指定数域*。例如，对于
$$
x^4-4
$$
在有理数域：
$$
x^4-4=(x^2-2)(x^2+2)
$$
实数域：
$$
x^4-4=(x-\sqrt2)(x+\sqrt2)(x^2+2)
$$
复数域：
$$
x^4-4=(x-\sqrt2)(x+\sqrt2)(x-\sqrt2i)(x+\sqrt2i)
$$
留意定理“不可约多项式”。

若p(x)是不可约多项式，那么对于任意$f(x),g(x)$，如果$p(x)\mid f(x)g(x)$，那么一定有$p(x)\mid f(x)$或$p(x)\mid g(x)$。

# 重因式

**不可约**多项式$p(x)$，如果满足$p^k(x)\mid f(x)$且$p^{k+1}(x)\nmid f(x)$，则称其为多项式$f(x)$的**k重公因式**。（上标k是指k次方的意思）

**微商**：导数

如果不可约多项式$p(x)$是多项式$f(x)$的k重饮食($k\geq1$)，那么它将是该多项式微商$f'(x)$的k-1重因式。

# 复系数和实系数多项式的因式分解

**代数基本定理**：每个次数$\geq1$的复系数多项式在复数域中有一个根。

**复系数多项式因式分解定理**：每个次数$\geq1$的复系数多项式在复数域上都可以*唯一*地分解成一次因式的乘积。即：
$$
f(x)=a_n(x-a_1)^{l_1}(x-a_2)^{l_2}...(x-a_s)^{a_s}
$$
其中，$a_i$是*不同的复数*，$l_i$是正整数。

下面我们来讨论实系数和复系数多项式的性质。

* 实数系数多项式

对于：
$$
f(x)=a_nx^n+a_{n-1}x^{n-1}+...+a_1x+a_0,~a_i\in\mathbb R
$$
如果存在复数$\alpha$, 使得$f(\alpha)=0$, 那么其共轭复数$\bar{\alpha}$, 将满足$f(\bar{\alpha})=0$。

> 即：
$$
\begin{aligned}
f(\alpha)&=&a_n\alpha^n&+a_{n-1}\alpha^{n-1}+...+a_1\alpha+a_0&=&0\\
\rightarrow f(\bar{\alpha})&=&a_n\bar{\alpha}^n&+a_{n-1}\bar{\alpha}^{n-1}+...+a_1\bar{\alpha}+a_0&=&0
\end{aligned}
$$

**实系数多项式因式分解定理**：每个次数$\geq1$的实系数多项式在实数域上都可以*唯一*地分解成一次因式与二次不可约因式的乘积。



# 有理系数多项式

对于：($Q$表示有理数集)
$$
f(x)=a_nx^n+a_{n-1}x^{n-1}+...+a_1x+a_0,~a_i\in\mathbb Q
$$
选取适当的整数c，总是能满足$cf(x)$成为整系数多项式。如果此时$cf(x)$各项系数有公因子，就可以提出来记作：
$$
cf(x)=dg(x)\rightarrow f(x)=\frac dcg(x)
$$
其中，$g(x)$是整系数多项式，并且各系数没有异于$\pm1$的公因子。

> 例如：
> $$
> \frac23x^4-2x^2-\frac25x=\frac25(5x^4-15x^2-3x)
> $$

如果一个非0整系数多项式$g(x)$，其系数没有异于$\pm1$的公因子（**互质**的意思）则称其为*本原多项式*。任何非零的有理系数多项式$f(x)$均可表示为一个有理数$r$和一个本原多项式$g(x)$的乘积:
$$
f(x)=rg(x)
$$

* 高斯引理：两个本原多项式的乘积还是本原多项式。
* **定理**-11：一非零整系数多项式，如果其能分解成两个次数较低的有理系数多项式的乘积，那么他一定能分解成两个次数较低的整系数多项式乘积。

> 证明：假设$f(x)=g(x)h(x)$, $f,g,h$均为有利系数多项式。
>
> 假设：$f(x)=af_1(x)$($a$可以是1，-1，$f_1(x)$是本原多项式)，此外讲g，h分解为有理数和本原多项式相乘的形式，即：
> $$
g(x)=rg_1(x),h(x)=sh_1(x)
$$
> 
> 从而：
>
$$
\begin{aligned}
af_1(x)&=rsg_1(x)h_1(x)\\\rightarrow
rs&=\pm a
\end{aligned}
$$
>
> 因此，rs的乘积将会是一个整数。因此：
$$
f(x)=\left(rsg_1(x)\right)h_1(x)
$$

除此以外，
